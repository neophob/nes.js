'use strict';

// The NES has two general-purpose controller ports
// generates a composite video signal with 240 lines of pixels, designed to be received by a television

class Input {
  constructor() {
  }

  foo() {
  }
}

module.exports = Input;
